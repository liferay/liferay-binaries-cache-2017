'use strict';

var _pluginLogger = require('liferay-npm-build-tools-common/lib/plugin-logger');

var _pluginLogger2 = _interopRequireDefault(_pluginLogger);

var _index = require('../index');

var _index2 = _interopRequireDefault(_index);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * © 2017 Liferay, Inc. <https://liferay.com>
 *
 * SPDX-License-Identifier: LGPL-3.0-or-later
 */

it('empties files when package is listed in imports', function () {
	var files = ['1.js', '2.js', '3.js'];

	(0, _index2.default)({
		pkg: {
			name: 'pkg-a',
			version: '1.0.0'
		},
		globalConfig: {
			imports: {
				provider: {
					'pkg-a': '^1.0.0'
				}
			}
		},
		config: {},
		log: new _pluginLogger2.default()
	}, {
		files: files
	});

	expect(files).toHaveLength(0);
});

it('leaves files untouched when package is missing from imports', function () {
	var originalFiles = ['1.js', '2.js', '3.js'];
	var files = originalFiles.slice();

	(0, _index2.default)({
		pkg: {
			name: 'pkg-a',
			version: '1.0.0'
		},
		globalConfig: {
			imports: {
				provider: {
					'other-package': '^1.0.0'
				}
			}
		},
		config: {},
		log: new _pluginLogger2.default()
	}, {
		files: files
	});

	expect(files).toEqual(originalFiles);
});
//# sourceMappingURL=index.test.js.map